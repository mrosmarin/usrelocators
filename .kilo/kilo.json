{
  "$schema": "https://app.kilo.ai/config.json",
  "mcp": {
    "linear": {
      "type": "local",
      "command": [
        "node",
        "{{LINEAR_PATH}}/build/index.js"
      ],
      "environment": {
        "LINEAR_API_KEY": "${env:LINEAR_API_KEY}"
      }
    }
  }
}
